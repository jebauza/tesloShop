STAGE=dev

DB_HOST=
DB_PORT=
DB_NAME=
DB_USERNAME=
DB_PASSWORD=
DB_SCHEMA=
DB_SSL=true

PORT=3000
HOST_API='http://localhost:3000/api'

JWT_SECRET=

ALLOWED_ORIGINS='http://localhost:4200,http://localhost:3000'

# Clave AES-256 para cifrado de datos sensibles en BD (64 caracteres hex = 32 bytes)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
